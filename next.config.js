/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'standalone' est utilisé pour Docker uniquement
  // Pour Vercel, cette option n'est pas nécessaire
}

module.exports = nextConfig
